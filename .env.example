# Used in production (Cloudflare Pages):
# your deployed backend URL (no trailing slash)
# Frontend public URL: https://sa.sujalpatil.dev
VITE_API_BASE_URL=https://your-backend-domain.example.com

# Optional: if omitted, frontend will reuse VITE_API_BASE_URL
VITE_SOCKET_URL=https://your-backend-domain.example.com

# Local dev only: Vite proxy target
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:3001
